# 变更记录

## 2026-09-19（北京时间 UTC+8）

- 任务：在 v2 全量审查基础上新建 `V2/自用覆写v3.js`，v2 原文保留不动
- 架构调整：
  - `SERVICE_SPECS` 成为唯一的平台配置面：`direct` / `head` / `auto` / `countries` / `loadBalance` / `lowRate` / `nodes` / `locked` / `gameNodes` / `defaultSelected` / `emptyFallback` / `enabledBy` 加 `rules` 来源；规则集名 `<组名>_Domain` / `_IP` / `_Custom` 与规则由规格生成，不再手写三处
  - 规则顺序由 `RULE_ORDER` 给出，阶段固定为 前置直连 → QUIC → 广告 → 零散规则 → 域名 → IP → MATCH；零散规则写在 `rules.extra`
  - 节点先做一次索引（地区集合、低倍率、游戏专线），分组只做集合查询；正则调用量降为原来的四分之一左右
  - 节点范围统一为 `nodes`：`manual` / `all` / `none` / 地区数组 / `ai`，`locked` 单独表示地区锁定，取代 v2 的 `regions` / `nodeRegions` / `expandNodes` / `noNodes`
  - 托管 DNS 改为白名单继承机场字段（`PRESERVED_DNS_KEYS`），不再先全量继承再删除
  - 校验新增：DNS 中 `#策略组` 与 `rule-set:` 引用、无引用规则集、无规则指向的服务组、规格字段合法性
- 洁净度：`Auto` / `Manual` 提为常量并新增 `autoNameOf`；`FakeIP_Filter` 改为 `managedOnly` 标记；第三方根地址统一进 `ROOTS`；识别表新增 `exclude` 字段处理“印度尼西亚”；移除字符串节点分支；`emptyFallback` 让 Bahamut 在无对应节点时退回 Auto 而非 REJECT；url-test 间隔统一 600 秒；头部注释改为设计要点
- 与 v2 差分（同一 121 节点输入）：策略组顺序与成员完全一致，仅 url-test 间隔变化；规则集仅 `1Password` 改名 `1Password_Custom`；规则集合一致，零散规则块前移到 AdBlock 之后；DNS 内容一致
- 测试：新增 `V2/test/run.js` 与 `V2/test/fixtures/nodes.txt`，运行 `node V2/test/run.js`
- 未改动：`Xiaohongsu` 组名保留，改名会重置客户端记忆；`DOWNLOAD_RULES` 仍为 Windows 进程名

## 2026-09-19 早前（同日）

- v2 本轮累计改动：GEOIP 移除、DNS 策略与 fake-ip-filter 按节点域名过滤、fallback 移除、进程识别补 strict；规则集全部改走 fastly.jsdelivr.net；国内分支前置直连、QUIC 屏蔽、default-selected、公共 DNS 名单同步、policy 折叠、EMBY 补充；机场 rule-providers / sub-rules 丢弃、悬空 dialer-proxy 清理、公共 DNS 匹配加边界、逗号多域名键；Grok 新增、OpenAI 更名 ChatGPT、AI 组默认 REJECT 并按各家官方支持范围过滤节点；地区识别表扩到 59 国；低倍率判定对齐节点排序脚本并仅出现在 10 个组；Steam / EPIC、抖音 / 小红书、Bahamut / Bilibili 成员按平台实际范围配置；AdBlock 组；面板顺序与 EMBY / YouTube 默认低倍率

## 2026-04-20 15:02（北京时间 UTC+8）

- 任务：修复自用.js 脚本在机场缺少某国家节点时配置校验失败的问题
- 改了什么：
  - 新增 `COUNTRY_GROUP_NAMES` 常量，定义国家分组名称集合（HK/TW/JP/SG/US）
  - 重写 `main` 函数，增加四步处理流程：构建分组 → 识别有效国家分组 → 清理服务分组中的无效引用 → 移除空国家分组
  - 重写 `buildGroup` 函数，当 `baseProxies` 和 `matchedNames` 都为空时，使用 `["REJECT"]` 作为 fallback 兜底
- 修复了什么：
  - 修复当机场没有某个国家/地区节点时（如没有台湾节点），对应国家分组（如 TW）因 `proxies` 和 `use` 字段同时为空，导致 Clash 配置校验报错 `proxy group[N]: TW: 'use' or 'proxies' missing` 的问题
  - 修复服务分组（如 Bahamut、Bilibili、OpenAI 等）引用了不存在的空国家分组名称导致的级联错误
- 更新了什么：
  - `自用.js` 第 646-725 行：新增 COUNTRY_GROUP_NAMES 常量、重写 main 函数和 buildGroup 函数
